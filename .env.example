# Application mode
APP_ENV=local

# Backend runtime (plain postgres:// or postgresql:// DSN, pgx-compatible)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/geetest_platform
REDIS_URL=redis://localhost:6379/0
SESSION_SECRET=change-me-session-secret
SESSION_TTL_SECONDS=604800
API_KEY_PEPPER=change-me-api-key-pepper
CDK_PEPPER=change-me-cdk-pepper
GEETEST_SOLVER_URL=https://solver.internal.example.com
GEETEST_SERVICE_API_KEY=change-me-geetest-service-api-key
SOLVER_CONNECT_TIMEOUT_SECONDS=10
SOLVER_READ_TIMEOUT_SECONDS=120
SOLVER_TOTAL_TIMEOUT_SECONDS=130

# Admission control per API key (fail-open on Redis errors)
RATE_LIMIT_PER_MINUTE=60
CONCURRENCY_LIMIT=4

# Administrator account. The API auto-creates this operator on first startup
# (only when admin_users is empty); cmd/admin-bootstrap can re-run it manually.
ADMIN_BOOTSTRAP_USERNAME=admin
ADMIN_BOOTSTRAP_PASSWORD=
ADMIN_SESSION_TTL_SECONDS=86400

# Frontend
VITE_API_BASE_URL=http://localhost:8000
